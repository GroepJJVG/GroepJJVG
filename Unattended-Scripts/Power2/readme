Power 2 or otherwise the ADDS and DNS installation. This mostly leaves the second script in tact and doesn't change it but does change the first script as it will add the configuration of the DNS after the promotion of the domain controller.

Otherwise this Power doesn't change much and is for the second period.
